export enum CodeEditorMode {
  VIEW = 'view',
  EDIT = 'edit',
}

export enum CodeEditorType {
  JSON = 'json',
  JAVA_SCRIPT = 'java-script',
}

export enum CodeEditorVariableType {
  STRING = 'STRING',
  STRING_ARRAY = 'STRING_ARRAY',
  NUMBER = 'NUMBER',
  DATE = 'DATE',
  DATE_ARRAY = 'DATE_ARRAY',
  BOOLEAN = 'BOOLEAN',
}
