import { MutateTextDirective } from '@directives/mutate-text.directive';

describe('MutateTextDirective', () => {
  it('should create an instance', () => {
    const directive = new MutateTextDirective();
    expect(directive).toBeTruthy();
  });
});
